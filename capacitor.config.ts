import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.lugraff.webknot',
  appName: 'WebKnot',
  webDir: 'docs',
  server: {
    androidScheme: 'https',
  },
};

export default config;
